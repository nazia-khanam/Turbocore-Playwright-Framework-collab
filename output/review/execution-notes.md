# Execution Notes

Do not run the generated tests until QA completes locators, routes, test data, and assertions. The stubs intentionally use TODO placeholders so they cannot validate the UI without manual completion.
