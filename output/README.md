# Playwright Stub Pack — Notifications

This zip contains a stub-only Playwright contribution for the TurboCore notifications feature.

## What is included
- `CHANGE-PLAN.md` — exact project delta and no-regression promise.
- `changed-files/` — only files added or modified for this workstream.
- `playwright-project/` — the complete persistent Playwright project after this addition.
- `review/` — QA completion notes and TODO lists.
- `scenarios/` — scenario traceability and row-to-test mapping.

## Important
The generated Playwright code is intentionally not runnable as-is. QA must complete all TODO locators, test data, navigation paths, trigger setup, and assertions before execution.
