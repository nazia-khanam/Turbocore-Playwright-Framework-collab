# Test Scenarios

The canonical scenario source is the uploaded `scenarios (4).md` file. The generated stub maps each row to a corresponding `TC-NOTIF-XX` Playwright test case in `tests/notifications.spec.ts`.
